import {createTheme} from "@mui/material/styles";

const Theme = createTheme({
    palette: {
        primary: {
            main: '#4C525C',
        },
        secondary: {
            main: '#30AB02',
        },
        error: {
            main: '#ff401a'
        }
    },
});
export default Theme;
